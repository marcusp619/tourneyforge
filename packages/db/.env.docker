# ─── Local Docker Dev Environment ───────────────────────────────────────────
# Copy this to .env to run drizzle-kit against local Docker Postgres.
#
#   cp packages/db/.env.docker packages/db/.env

DATABASE_URL=postgresql://tf:tfpassword@localhost:5432/tourneyforge
